import type { SiteConfig } from '@types';

export const siteConfig: SiteConfig = {
  /* The name of the website. Used in page titles and feeds. */
  title: 'tomherni.dev',

  /* The description of the website. Used in page meta tags and feeds. */
  description: 'Personal website of Tom Herni.',

  /* Author of posts. Used in feeds. */
  author: {
    name: 'Tom Herni',
    email: 'tomherni@gmail.com',
  },
};
